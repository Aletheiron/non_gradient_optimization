# Non-gradient learning for neural networks

We can find local min for loss functions without computing gradients.
We change weights at the same degree, if the latest changes were good for our loss or utility function
